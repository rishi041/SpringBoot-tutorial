spring.application.name=springrest
# changing server port 
server.port=${PORT:9090}

# PostgreSQL connection
# spring.datasource.url=jdbc:postgresql://ep-dawn-rice-adargg8v-pooler.c-2.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_phSJWRV1Be4k&sslmode=require&channelBinding=require
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
# Optional: Boot auto-detects Postgres; remove to silence the deprecation warning
#  spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
